# MNIST data

If you want to use the `MnistHandler` 
from `data_handlers.py`, put the mnist
training, validation and test data here.

The file names should be `mnist-identifier-data.csv`
resp. `mnist-identifier-labels.csv` and contain
space separated data, where each row is one sample.